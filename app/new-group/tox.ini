[tox]
envlist =
    ; py36
    ; c0
    c1
	; lint

skip_missing_interpreters = true

[tool:pytest]
minversion = 3.0
testpaths = tests

[coverage:run]
source =
    new_group
    tests

[coverage:paths]
source =
    new_group
    tests
    .tox/*/lib/python*/site-packages/new_group
    .tox/*/lib/python*/site-packages/new_group/tests

[flake8]
max-line-length = 119
exclude =
    .git
    __pychache__
    .tox
    venv

[isort]
profile=black

[tool:isort]
line_length = 119

[mypy]
python_version = 3.6
warn_return_any = True
warn_unused_configs = True

[mypy-flask]
ignore_missing_imports = True

[testenv]
passenv = LANG
deps =
    pytest>=3
commands =
   pytest -v --basetemp="{envtmpdir}" {posargs}

setenv =
    PYTHONPATH = {toxinidir}/new_group

[testenv:c0]
passenv = LANG
deps =
    pytest>=3
    pytest-cov
commands =
   pytest --cov=new_group tests -v --cov-report=term --basetemp="{envtmpdir}" {posargs}

setenv =
    PYTHONPATH = {toxinidir}/new_group

[testenv:c1]
passenv = LANG
setuptools_version = 57.5.0
deps =
    pytest>=3
    pytest-cov
	-rrequirements.txt
commands =
   pytest --cov=new_group tests -v -vv -s --cov-branch --cov-report=term  --basetemp="{envtmpdir}" {posargs}

setenv =
    PYTHONPATH = {toxinidir}/new_group

[testenv:lint]
passenv = LANG
deps =
	black
	isort
	flake8
	mypy
commands =
	black .
	isort .
	flake8 .
	mypy new_group
